server:
  port: 9006


spring:
  datasource:
    url: jdbc:mysql://localhost:3307/javatechie
    username: query
    password: query
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    show-sql: true
    hibernate.ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect